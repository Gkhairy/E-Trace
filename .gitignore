*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# graphify knowledge-graph output (build artifact)
/graphify-out

# Impeccable design tool — installed via `npx impeccable install`, not vendored
/.claude/skills/
# Per-machine Claude Code settings (local permissions, not shared)
/.claude/settings.local.json

# Remix-generated Solidity build outputs (not source)
/contracts/.deps/
/contracts/artifacts/

# Catatan deploy pribadi — tidak untuk repo
DEPLOY-RAILWAY.md
